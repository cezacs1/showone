        private static void automax()
        {
            new Thread
                while (*cheat run*)
                {
                    if (!connected) contiune;

                    for (int i = 0; i < 64; i++)
                    {
                       if (entity health == 0) continue;
                       last++;
                    }
                    maxplayer = last + 1; // +1 = tolerans
                    
                    Thread.Sleep(5000);
                }
            Start;
        }
